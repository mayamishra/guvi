#include<stdio.h>
#include<ctype.h>
#include<string.h>
int main()
{
    char str[100000],rev[100000];
    scanf("%s",str);
    for(int i=strlen(str)-1,j=0;i>=0,j<=strlen(str)-1;i--,j++)
    {
        rev[aj]=str[i];
        printf("%c",rev[j]);
    }
    
    
}
